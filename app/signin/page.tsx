"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
import "../globals.css";
import "./signin.css";
type Step = "start" | "otp" | "password" | "done";
export default function SignInPage() {
  const [step,setStep]=useState<Step>("start"); const [identifier,setIdentifier]=useState(""); const [password,setPassword]=useState(""); const [confirm,setConfirm]=useState(""); const [error,setError]=useState("");
  function sendOtp(e:FormEvent){e.preventDefault();if(password&&localStorage.getItem("dyc_demo_password")===password){setError("");setStep("done");return}setError("");setStep("otp")}
  function verifyOtp(e:FormEvent){e.preventDefault();const code=(e.currentTarget.elements.namedItem("otp") as HTMLInputElement).value;if(code!=="000000"){setError("In Demo mode, use 000000 as the verification code.");return}setError("");setStep("password")}
  function savePassword(e:FormEvent){e.preventDefault();if(password.length<8)return setError("Use at least 8 characters for your password.");if(password!==confirm)return setError("The passwords do not match.");localStorage.setItem("dyc_demo_password",password);setError("");setStep("done")}
  return <main><nav className="nav"><Link className="brand" href="/"><span className="brand-mark">DG</span><span>DoingGood <b>Youth Connect</b></span></Link><Link className="text-link" href="/">← Public platform</Link></nav><section className="signin-wrap"><div className="signin-intro"><p className="eyebrow yellow">Secure account access</p><h1>Come back to your community.</h1><p>Use your email or phone to access your profile, opportunities, workspace voucher, or Ambassador tools.</p><div className="access-list"><span><b>Public</b> Browse civic knowledge, stewardship, stories, and opportunities.</span><span><b>Members</b> Save ideas, submit testimonials, and access benefits.</span><span><b>Ambassadors</b> Use sharing tools and view contribution metrics.</span></div></div><div className="signin-card">{step==="done"?<div className="success"><b>You’re signed in.</b><span>Your trusted-device session can last up to 90 days. Sensitive actions may ask for a fresh OTP.</span><Link className="button primary" href="/profile">Continue to my profile →</Link></div>:step==="password"?<form onSubmit={savePassword}><p className="eyebrow blue">Create your password</p><h2>Protect your account.</h2><p className="otp-help">Your OTP is verified. Create a password for future sign-ins.</p><label>Password<input type="password" required minLength={8} value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 8 characters" /></label><label>Confirm password<input type="password" required minLength={8} value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Repeat your password" /></label>{error&&<p className="form-error">{error}</p>}<button className="button primary" type="submit">Save password <span>→</span></button></form>:step==="otp"?<form onSubmit={verifyOtp}><p className="eyebrow blue">Verify one-time code</p><h2>Enter your OTP.</h2><p className="otp-help">We sent a six-digit code to <b>{identifier}</b>.</p><div className="demo-notice">Demo mode: use <b>000000</b>. Live OTP delivery is not enabled.</div><label>One-time password<input name="otp" inputMode="numeric" pattern="[0-9]{6}" maxLength={6} required placeholder="000000" /></label>{error&&<p className="form-error">{error}</p>}<button className="button primary" type="submit">Verify and continue <span>→</span></button><button className="text-button" type="button" onClick={()=>setStep("start")}>Use a different email or phone</button></form>:<form onSubmit={sendOtp}><p className="eyebrow blue">Sign in securely</p><h2>Welcome back.</h2><label>Email or phone number<input required value={identifier} onChange={e=>setIdentifier(e.target.value)} placeholder="you@example.com or 0800 000 0000" /></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your password" /></label><button className="button primary" type="submit">Continue with OTP <span>→</span></button><small>For this Demo, OTP verifies the account before password setup. A live account service will be connected only when you instruct us to switch from Demo.</small></form>}</div></section><section className="signin-note"><p><b>No account yet?</b> <Link href="/join">Join DoingGood Youth Connect</Link> voluntarily. Public civic information remains available without signing in.</p></section></main>;
}
